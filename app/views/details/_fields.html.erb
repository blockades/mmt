<fieldset>
  <%= f.label :coin %>
  <%= f.collection_select(:coin_id, Coin.all, :id, :code) %>
  <%# %%TODO%% work out correct delimitation to 2 decimal places %>
  <%= f.label :rate %>
  <%= f.number_field :rate, value: number_to_currency(f.object.rate.to_f, delimiter: '', unit: ''), step: :any %>
</fieldset>
