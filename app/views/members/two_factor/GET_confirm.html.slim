h2 Confirm

p You are here because you have begun the setup for two factor authentication

p Scan the QR code and enter the code to confirm OTP authentication
= google_authenticator_qrcode
br
= form_for :two_factor, url: confirm_two_factor_members_path, method: :patch do |f|
  = f.text_field :code
  = f.submit "Confirm"
