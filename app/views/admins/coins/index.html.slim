table
  thead
    th Name
    th Code
    th Liability
    th Available
    th Total
    th BTC rate (bittrex)
    th Actions
  tbody
    - @coins.each do |coin|
      tr
        td= coin.name
        td= coin.code
        td= coin.assigned_holdings
        td= coin.availability
        td= coin.total_in_system
        td= coin.btc_rate
        td
          .icons
            = link_to admins_new_coin_deposit_path(coin), class: "icon", title: "Load" do
              i.fa.fa-level-up.fa-lg
            = link_to admins_new_coin_allocation_path(coin), class: "icon", title: "Allocate to Member" do
              i.fa.fa-user-plus.fa-lg
